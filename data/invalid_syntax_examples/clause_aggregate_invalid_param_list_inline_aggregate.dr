; The use of an aggregating functor can be paired with a predicate or an
; inline-defined predicate. When using an inline-defined predicate, the paramer
; list must be a comma-separate list of type and variable names.

#functor sum(aggregate i32 X, summary i32 Y)

#message input(i32 X)
#message output(i32 X)

output(Y) : sum(X, Y) over (i32 X Z) { input(X) }.
