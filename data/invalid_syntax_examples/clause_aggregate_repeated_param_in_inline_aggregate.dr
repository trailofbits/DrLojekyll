; The use of an aggregating functor can be paired with a predicate or an
; inline-defined predicate. When using an inline-defined predicate, the
; parameter list must not re-declare the same parameter twice.

#functor sum(aggregate i32 X, summary i32 Y)

#message input(i32 X)
#message output(i32 X)

output(Y) : sum(X, Y) over (i32 X, i32 X) { input(X) }.
